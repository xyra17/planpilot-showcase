# PlanPilot 第二阶段：分块检索与质量评估报告

**实施日期**：2026-07-25  
**阶段状态**：完成
**前置提交**：`638e40d chore: close phase-one audit and route AI models`

## 1. 阶段目标

第二阶段解决第一阶段遗留的“整份文档只有一个向量、仅索引前 8,000
字符、结果无法定位原文段落”问题，范围包括：

- 文档分块
- 批量 Embedding
- 分块级向量与关键词检索
- 可展示的引用来源
- Recall@K 与 MRR 检索质量评估
- 旧资料渐进式重建索引

## 2. 数据模型与迁移

新增迁移：

`backend/alembic/versions/i3j4k5l6m7n8_add_knowledge_chunks.py`

`backend/alembic/versions/j4k5l6m7n8o9_migrate_embeddings_to_1024.py`

新增 `knowledge_chunks` 表：

| 字段 | 用途 |
|---|---|
| `item_id` | 关联原始知识文档，删除文档时级联删除 |
| `chunk_index` | 文档内稳定段号 |
| `content` | 分块正文 |
| `start_char` / `end_char` | 在原文中的字符范围 |
| `embedding` | Qwen3 原生 1024 维向量 |
| `created_at` | 建立索引时间 |

约束与索引：

- `(item_id, chunk_index)` 唯一约束
- `item_id` 普通索引
- `embedding` HNSW + cosine 索引
- SQLAlchemy 模型显式声明 HNSW 索引，`alembic check` 无模型漂移

迁移验证：

- 正式本地库：已迁移到 `j4k5l6m7n8o9 (head)`
- 临时空库：从初始迁移完整执行到分块迁移通过
- 验证后已删除临时数据库

## 3. 分块与批量向量化

默认参数：

- 目标分块长度：1,200 字符
- 相邻重叠：200 字符
- Embedding 批大小：32
- 最大提取正文：沿用第一阶段的 50,000 字符限制

分块优先在段落、换行或句子边界结束，同时记录原文字符范围。文档重新处理前会删除旧分块，避免重复索引。

Embedding 返回数量和向量维度均会校验。任一批次失败时继续尝试下一个兼容服务；所有服务失败后沿用第一阶段的 Celery 重试和失败可见机制。

原 `knowledge_items.embedding` 暂时保留第一段向量，用于兼容旧代码和滚动升级；新搜索优先使用 `knowledge_chunks`。

## 4. 检索与引用

检索顺序：

1. 分块级余弦向量检索
2. 未重建旧资料的整文档向量检索
3. 分块级关键词检索
4. 旧资料整文档关键词检索

向量最低相关度仍为 0.3。搜索继续执行用户所有权和可选目标过滤。

搜索结果新增：

- `source_type`
- `source_url`
- `chunk_index`
- `start_char`
- `end_char`
- `citation`，例如 `CPA讲义.pdf · 第 3 段`

旧调用方原有的 `id`、`title`、`snippet`、`score`、`goal_id` 和
`kb_id` 均保持兼容。

## 5. 旧资料重建

新增：

`POST /api/v1/knowledge/reindex?limit=100`

接口只处理当前用户、状态为 `ready`、来源为上传或 URL、且尚无分块的资料。
每批最多 500 项，任务进入现有 Celery 队列；已分块资料不会重复派发。

## 6. 检索质量评估

新增：

`POST /api/v1/knowledge/search/evaluate`

请求示例：

```json
{
  "cases": [
    {
      "query": "资产负债表的确认条件",
      "expected_item_ids": ["知识文档 ID"]
    }
  ],
  "limit": 5,
  "goal_id": null
}
```

输出：

- 每个问题的召回文档顺序
- 单问题 Recall@K
- 单问题 Reciprocal Rank
- 数据集平均 Recall@K
- Mean Reciprocal Rank（MRR）

该接口只在当前用户资料内检索，不暴露其他用户文档。

## 7. 验证结果

| 验证项 | 结果 |
|---|---|
| Ruff | All checks passed |
| 后端单元测试 | 66/66 passed |
| 分块、引用与知识专项测试 | 22/22 passed |
| 真实 PostgreSQL / Redis / DeepSeek 集成测试 | 55/55 passed，255.27 秒 |
| Next.js 生产构建 | passed，17 个路由 |
| 正式本地库迁移 | `j4k5l6m7n8o9 (head)` |
| 空库完整迁移链 | passed |
| `alembic check` | No new upgrade operations detected |
| `datetime.utcnow()` | 后端调用已全部移除，相关弃用警告消失 |
| pytest 进程退出 | 正常，退出码 0 |

新增测试覆盖：

- 分块正文与原文字符范围一致
- 相邻分块保留重叠
- 非法分块参数被拒绝
- 超过 32 段时按批调用并保持结果顺序
- 分块关键词搜索返回段号和引用
- Recall@K / MRR 评估结果
- 旧 `ready` 文档可进入重建队列
- 知识集成文件可独立运行，不依赖前序文件创建目标
- Redis 测试客户端按测试事件循环释放，无跨循环 teardown 错误

## 8. 本地 Embedding 运行环境

最终选型：

- 模型：`Qwen3-Embedding-0.6B`
- 格式：官方 GGUF Q8_0
- 模型大小：639.15 MB
- 原生向量维度：1024
- 服务：LM Studio 自带 llama.cpp 运行时
- 接口：OpenAI-compatible `/v1/embeddings`

模型归档：

- `/Users/Admin/Downloads/models/PlanPilot/Qwen/Qwen3-Embedding-0.6B-GGUF/`

macOS launchd 受 Downloads 隐私权限限制，因此后台服务使用校验一致的运行副本：

- `/Users/Admin/Library/Application Support/PlanPilot/models/`

两份文件 SHA-256 一致。后台服务：

- LaunchAgent：`~/Library/LaunchAgents/com.planpilot.embedding.plist`
- 服务标签：`com.planpilot.embedding`
- 监听端口：1234
- API Key：本地占位值 `local`
- 自动启动与异常重启：已启用
- 日志：`~/Library/Logs/PlanPilot/`

项目配置：

```dotenv
EMBEDDING_API_KEY=local
EMBEDDING_BASE_URL=http://localhost:1234/v1
EMBEDDING_MODEL_NAME=qwen3-embedding-0.6b
EMBEDDING_DIMENSIONS=1024
```

Docker Compose 覆盖服务地址为
`http://host.docker.internal:1234/v1`，API 和 Worker 均已重新创建并验证。

真实端到端验证：

- 宿主机调用：返回 1024 维
- Docker API 调用：返回 1024 维
- 项目 `_process()`：3,934 字长文档生成 4 个分块
- 4 个分块均保存 1024 维向量
- 分块字符范围与原文一致
- 文档最终状态：`ready`
- 临时用户、文档与分块：验证后全部删除

## 9. 阶段结论

第二阶段的代码、数据库迁移、分块向量化、降级链路、引用结构和评估能力
已经完成。Qwen3 本地 Embedding 已通过宿主机、Docker 和项目处理任务三层真实验证。

因此第二阶段可以正式关闭。后续质量工作是建立 20～50 条人工标注问题集，
记录首个 Recall@K / MRR 基线，并按结果调整分块长度、重叠量和召回阈值。
