# 前端历史构建快照归档

归档日期：2026-08-08

本目录曾保存原先散落在 `frontend` 根目录中的 `.next.previous*`、`.next-stale-*`、`.next-before-*` 和 `frontend.zip`。这些历史构建产物已于 2026-08-20 清理，本目录只保留说明文件。

当前可运行产物仍由 `frontend/.next` 生成，且不应提交进 Git。清理这些构建产物不会影响当前项目源码。
