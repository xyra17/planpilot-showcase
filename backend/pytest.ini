[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
markers =
    integration: 集成测试（需要真实 PostgreSQL + Redis）
    soak: 重复并发与故障恢复发布门槛测试
    live_llm: 调用真实模型供应商的非确定性验收测试
